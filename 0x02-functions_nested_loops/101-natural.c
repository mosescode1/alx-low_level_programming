#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	long long int sum;
	int i;

	for (i == 0; i <= 1024; i++)
	{
		if (i = 3 || i = 5)
		{
			sum += i;
		}
	}
	return (0);
}	
